# 🩸 Blood Report Analyzer – Groq + TiDB Cloud

A Streamlit app that parses blood test reports, saves results to TiDB Cloud, and enables AI-powered Q&A using Groq + LangChain.

---

## 🚀 Setup Instructions

### 1. Install dependencies
```bash
pip install -r requirements.txt
